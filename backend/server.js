require('dotenv').config();const express=require('express'),cors=require('cors'),db=require('./config/db'),error=require('./middleware/error');const app=express();app.use(cors());app.use(express.json());app.get('/api/health',(req,res)=>res.json({ok:true}));app.use('/api/auth',require('./routes/auth'));app.use('/api/tickets',require('./routes/tickets'));app.use('/api/analytics',require('./routes/analytics'));app.use('/api/users',require('./routes/users'));app.use('/api/feedback',require('./routes/feedback'));app.use('/api/sla',require('./routes/sla'));app.use(error);db().then(()=>app.listen(process.env.PORT||5000,()=>console.log('Backend running'))).catch(console.error);