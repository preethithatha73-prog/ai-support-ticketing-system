require('dotenv').config();const db=require('./config/db'),User=require('./models/User'),Ticket=require('./models/Ticket'),SLA=require('./models/SLAConfig'),bcrypt=require('bcryptjs');(async()=>{await db();await Promise.all([User.deleteMany(),Ticket.deleteMany(),SLA.deleteMany()]);const p=await bcrypt.hash('Password123!',10);const users=await User.insertMany([{name:'Admin',email:'admin@example.com',password:p,role:'admin'},{name:'Agent One',email:'agent1@example.com',password:p,role:'agent',department:'Technical'},{name:'Agent Two',email:'agent2@example.com',password:p,role:'agent',department:'Billing'},{name:'Customer One',email:'customer1@example.com',password:p,role:'customer'},{name:'Customer Two',email:'customer2@example.com',password:p,role:'customer'}]);const customer=users.find(x=>x.role==='customer'),agent=users.find(x=>x.role==='agent');await SLA.insertMany([{priority:'Critical',resolutionHours:2},{priority:'High',resolutionHours:4},{priority:'Medium',resolutionHours:8},{priority:'Low',resolutionHours:24}]);await Ticket.insertMany([{ticketNumber:'T-DEMO-001',subject:'Cannot login',description:'Password reset is not working',customer:customer._id,assignedAgent:agent._id,category:'Login',priority:'High',slaDeadline:new Date(Date.now()+14400000)},{ticketNumber:'T-DEMO-002',subject:'Invoice question',description:'Please explain my latest charge',customer:customer._id,assignedAgent:agent._id,category:'Billing',priority:'Medium',slaDeadline:new Date(Date.now()+28800000)}]);console.log('Seed complete');process.exit()})().catch(e=>{console.error(e);process.exit(1)})